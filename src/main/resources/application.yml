server:
  port: 80

spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ronilsonalves/spring-cloud-config.git
          default-label: main
eureka:
  client:
    service-url:
      defaultZone: https://eurekaserver.perseidas.dev/eureka